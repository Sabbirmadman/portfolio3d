import { Html } from "@react-three/drei";

const Loader = () => {
    return (
        <Html center>
            <span className="canvas-load">Loading...</span>
        </Html>
    );
};

export default Loader;
